# Development dependencies (runtime deps: NONE — the cairn core is stdlib-only by design).
# Setup:
#   python3.11 -m venv .venv
#   .venv/bin/pip install -r requirements-dev.txt
#   .venv/bin/pip install -e packages/cairn -e packages/cairn-retrieval
pytest>=8
pytest-cov>=5
ruff>=0.5
mypy>=1.10
